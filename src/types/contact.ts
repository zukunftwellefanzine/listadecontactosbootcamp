export interface Contact {
  id: number;
  name: string;
  phone: string;
  email: string;
  address: string;
}

export interface ContactFormData {
  name: string;
  phone: string;
  email: string;
  address: string;
}